// Documentation metadata. Rendered by <ApiReference> on the docs page and
// served as structured data at /r/ai/<name>.json.
import type { ComponentMeta } from '../schema.js';

export const cameraPreviewMeta: ComponentMeta = {
  name: 'camera-preview',
  usage: {
    import: "import { CameraPreview } from '@/components/ui/camera-preview';",
    snippet: '<CameraPreview />',
  },
  accessibility: {
    summary:
      'The Camera Preview component is built with accessibility in mind:',
    items: [
      'Screen reader support for all interactive elements',
      'High contrast mode support',
      'Voice-over announcements for capture events',
      'Keyboard navigation support where applicable',
      'Proper focus management throughout the interface',
    ],
  },
};
