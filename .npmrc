# React Native / Expo require a flat-ish node_modules for Metro to resolve
# peer deps correctly across workspace packages.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
