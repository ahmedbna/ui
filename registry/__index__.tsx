import { accordionRegistry } from '@/registry/registry/accordion';
import { actionSheetRegistry } from '@/registry/registry/action-sheet';
import { alertRegistry } from '@/registry/registry/alert';
import { audioPlayerRegistry } from '@/registry/registry/audio-player';
import { audioRecorderRegistry } from '@/registry/registry/audio-recorder';
import { audioWaveformRegistry } from '@/registry/registry/audio-waveform';
import { avatarRegistry } from '@/registry/registry/avatar';
import { badgeRegistry } from '@/registry/registry/badge';
import { bottomSheetRegistry } from '@/registry/registry/bottom-sheet';
import { buttonRegistry } from '@/registry/registry/button';
import { cameraPreviewRegistry } from '@/registry/registry/camera-preview';
import { cameraRegistry } from '@/registry/registry/camera';
import { cardRegistry } from '@/registry/registry/card';
import { carouselRegistry } from '@/registry/registry/carousel';
import { checkboxRegistry } from '@/registry/registry/checkbox';
import { collapsibleRegistry } from '@/registry/registry/collapsible';
import { colorPickerRegistry } from '@/registry/registry/color-picker';
import { comboboxRegistry } from '@/registry/registry/combobox';
import { datePickerRegistry } from '@/registry/registry/date-picker';
import { filePickerRegistry } from '@/registry/registry/file-picker';
import { galleryRegistry } from '@/registry/registry/gallery';
import { helloWaveRegistry } from '@/registry/registry/hello-wave';
import { iconRegistry } from '@/registry/registry/icon';
import { imageRegistry } from '@/registry/registry/image';
import { inputOtpRegistry } from '@/registry/registry/input-otp';
import { linkRegistry } from '@/registry/registry/link';
import { mediaPickerRegistry } from '@/registry/registry/media-picker';
import { modeToggleRegistry } from '@/registry/registry/mode-toggle';
import { onboardingRegistry } from '@/registry/registry/onboarding';
import { parallaxScrollViewRegistry } from '@/registry/registry/parallax-scrollview';
import { pickerRegistry } from '@/registry/registry/picker';
import { popoverRegistry } from '@/registry/registry/popover';
import { progressRegistry } from '@/registry/registry/progress';
import { radioRegistry } from '@/registry/registry/radio';
import { scrollViewRegistry } from '@/registry/registry/scroll-view';
import { searchbarRegistry } from '@/registry/registry/searchbar';
import { shareRegistry } from '@/registry/registry/share';
import { sheetRegistry } from '@/registry/registry/sheet';
import { skeletonRegistry } from '@/registry/registry/skeleton';
import { spinnerRegistry } from '@/registry/registry/spinner';
import { switchRegistry } from '@/registry/registry/switch';
import { tableRegistry } from '@/registry/registry/table';
import { tabsRegistry } from '@/registry/registry/tabs';
import { textRegistry } from '@/registry/registry/text';
import { toastRegistry } from '@/registry/registry/toast';
import { toggleRegistry } from '@/registry/registry/toggle';
import { videoRegistry } from '@/registry/registry/video';
import { viewRegistry } from '@/registry/registry/view';
import { separatorRegistry } from '@/registry/registry/separator';

export const Index: Record<string, any> = {
  ...accordionRegistry,
  ...actionSheetRegistry,
  ...alertRegistry,
  ...audioPlayerRegistry,
  ...audioRecorderRegistry,
  ...audioWaveformRegistry,
  ...avatarRegistry,
  ...badgeRegistry,
  ...bottomSheetRegistry,
  ...buttonRegistry,
  ...cameraPreviewRegistry,
  ...cameraRegistry,
  ...cardRegistry,
  ...carouselRegistry,
  ...checkboxRegistry,
  ...collapsibleRegistry,
  ...colorPickerRegistry,
  ...comboboxRegistry,
  ...datePickerRegistry,
  ...filePickerRegistry,
  ...galleryRegistry,
  ...helloWaveRegistry,
  ...iconRegistry,
  ...imageRegistry,
  ...inputOtpRegistry,
  ...linkRegistry,
  ...mediaPickerRegistry,
  ...modeToggleRegistry,
  ...onboardingRegistry,
  ...parallaxScrollViewRegistry,
  ...pickerRegistry,
  ...popoverRegistry,
  ...progressRegistry,
  ...radioRegistry,
  ...scrollViewRegistry,
  ...searchbarRegistry,
  ...shareRegistry,
  ...sheetRegistry,
  ...skeletonRegistry,
  ...spinnerRegistry,
  ...switchRegistry,
  ...tableRegistry,
  ...tabsRegistry,
  ...textRegistry,
  ...toastRegistry,
  ...toggleRegistry,
  ...videoRegistry,
  ...viewRegistry,
  ...separatorRegistry,
};
